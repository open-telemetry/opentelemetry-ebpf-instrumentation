name: oats proxy
interval: 500ms
fixture:
  compose:
    files:
      - ../docker-compose-proxy.yml
      - ../../weaver/docker-compose-weaver.yml
seed:
  type: app
input:
  - path: /smoke
expected:
  traces:
    - traceql: '{ .http.route = "/smoke" && .server.port=8080}'
      match_spans:
        - name: GET /smoke
