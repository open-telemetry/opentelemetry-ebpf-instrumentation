services:
  obi-config-migrate:
    image: hatest-obi
    entrypoint:
      - /bin/sh
      - -c
    command:
      - /obi config migrate /configs/obi-config-go-otel-grpc.yml > /coverage/obi-config-go-otel-grpc-v2.yml
    volumes:
      - ./configs/:/configs:ro
      - ../../../testoutput:/coverage

  obi:
    command:
      - --config=/coverage/obi-config-go-otel-grpc-v2.yml
    depends_on:
      obi-config-migrate:
        condition: service_completed_successfully
