routes:
  unmatched: path
otel_traces_export:
  endpoint: http://jaeger:4318
discovery:
  instrument:
    - exe_path: "*/python*"
      containers_only: true
attributes:
  select:
    "*":
      include: ["*"]
